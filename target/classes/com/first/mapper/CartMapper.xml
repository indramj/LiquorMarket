<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.first.mapper.CartMapper">
	
	<!-- 카트 추가 -->
	<insert id="addCart">

		insert into vam_cart(memberId, drinkId, drinkCount)
		values(#{memberId},
		#{drinkId}, #{drinkCount})

	</insert>


	<!-- 카트 삭제 -->
	<delete id="deleteCart">

		delete from table_cart where cartId = #{cartId}

	</delete>

	<!-- 카트 수량 수정 -->
	<update id="modifyCount">

		update drink_cart set drinkCount=#{drinkCount} where cartId = #{cartId}

	</update>


	<!-- 카트 목록 -->
	<select id="getCart" resultType="com.first.domain.CartDTO">

		select a.cartId, a.memberId, a.drinkId, a.drinkCount, b.drinkName, b.drinkPrice,
		from table_cart a left outer join table_drink b on a.drinkId = b.drinkId
		where memberId = #{memberId}

	</select>

	<!-- 카트 확인 -->
	<select id="checkCart" resultType="com.first.domain.CartDTO">

		select * from table_cart
		where memberId = #{memberId} and drinkId = #{drinkId}

	</select>
</mapper>